# 🌊 LINE 浪況機器人 — 簡報大綱（PPT Storyboard）

> **建議時長**：15–20 分鐘 + Q&A 5 分鐘  
> **總頁數**：18 張  
> **風格建議**：深藍底色 + 白字 + 海浪/衝浪圖示，顏色參考：`#0D4F8C`（深藍）/ `#6AC6F2`（淺藍）/ `#F2604C`（珊瑚紅）

---

## 📑 簡報結構總覽

```
Part 0   封面（1 張）
Part 1   為什麼做這個？（2 張）   ← 說服評審這個問題值得解決
Part 2   這個系統做了什麼？（2 張） ← 功能 + Demo 截圖
Part 3   怎麼設計的？（8 張）     ← 技術核心，報告重點
Part 4   踩了哪些坑？（2 張）     ← 展現解決問題的能力
Part 5   未來 + 結尾（3 張）
```

---

## 🖼 逐張內容

---

### SLIDE 01｜封面

```
標題：LINE 浪況自動推播機器人
副標：結合 CWA 浮標 API × LINE Messaging × 衝浪科學的智慧推播系統

作者姓名 ／ 課程名稱 ／ 日期

背景建議：衝浪照片（海浪 + 陽光）
角落放 LINE Bot QR Code（可掃描試用）
```

---

### SLIDE 02｜目錄

```
1. 問題背景與動機
2. 系統功能展示
3. 系統架構設計
4. 資料來源與容錯機制
5. 浪況評分邏輯
6. LINE 整合技術
7. 推播排程設計
8. 開發挑戰與解決方案
9. 成果與未來規劃
```

---

### SLIDE 03｜問題背景

**標題：衝浪者每天面對的痛點**

```
左側（問題描述）：
  📍 台灣有 15+ 個主要衝浪浪點
  🔍 資訊分散在 CWA、GoOcean、Windy 等多個網站
  📱 每天要手動查詢、沒有個人化建議
  ⚠️ 看不懂氣象術語的衝浪者容易誤判安全性

右側（現有痛點圖解）：
  [圖示] 用戶 → 打開 CWA → 打開 GoOcean → 打開 Windy → 自己判斷
         這個流程太繁瑣
```

**底部說明文字**：
> 「能不能讓機器人每天早上主動告訴我：今天能不能衝？去哪裡最好？」

---

### SLIDE 04｜解決方案 — 系統定位

**標題：打造衝浪者的私人氣象教練**

```
三欄對比：

  傳統做法          本系統           升級效果
  ─────────        ─────────        ─────────
  手動查網站   →   自動推播          省時、不遺漏
  看數字      →   看星號＋燈號       一眼懂
  通用資訊    →   依浪齡個人化       初學者不冒險
  等到了才知道 →   長浪警戒主動通知   提前準備
```

---

### SLIDE 05｜功能展示（Demo）

**標題：機器人可以做什麼？**

```
左半部（功能列表）：                右半部（LINE 截圖）：

  📢 每日 05:00 / 15:00 廣播          [截圖：早報推播訊息]
  🔍 輸入地名即時查詢浪況
  🏆 今日最佳浪點前三名推薦           [截圖：最佳浪點推薦]
  ⚠️ 大浪 / 颱風自動警戒通知
  📺 一鍵查 YouTube 浪況直播           [截圖：圖文選單 + Flex 卡片]
  👤 建立個人 Surfer 檔案
```

---

### SLIDE 06｜整體系統架構圖

**標題：系統架構總覽**

```
[大圖：將 ARCHITECTURE.md 的 ASCII 圖轉為視覺化方塊圖]

推薦用三個橫向區域呈現：

┌──────────────┐    ┌──────────────────┐    ┌──────────────┐
│   資料來源    │ →  │  Render 伺服器   │ →  │  使用者端    │
│              │    │                  │    │              │
│ CWA 浮標 API │    │ broadcast.py     │    │ LINE App     │
│ Open-Meteo  │    │ webhook.py       │    │              │
│ CWA 潮汐 API │    │ line_api.py      │    └──────────────┘
│ CWA 颱風 API │    │ db.py            │
└──────────────┘    └──────┬───────────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
         Supabase    LINE API      GitHub Actions
         (資料庫)    (推播/回覆)    (定時排程)
```

底部：「共 5 個核心模組、4 個外部服務、4 個 GitHub Actions 工作流程」

---

### SLIDE 07｜技術選型

**標題：為什麼選這些技術？**

```
技術          選擇理由                          替代方案比較

Python Flask  輕量 Webhook 伺服器               Django（太重）
              Render 原生支援 gunicorn

LINE API      台灣用戶覆蓋率 > 90%              Telegram、FB Messenger
              支援 Push / Flex Message / 圖文選單

CWA API       台灣官方浮標，有即時波高/週期/風向  無同等品質的替代品
              免費，需申請 API Key

Supabase      免費雲端 PostgreSQL               Firebase（較貴）
              Render 重啟後資料不消失            本機 SQLite（Render 上不持久）

GitHub Actions 免費 CI/CD + 定時任務            Render 付費 Cron Job
               不受 Render 睡眠機制影響
```

---

### SLIDE 08｜資料來源設計

**標題：CWA 浮標 API × Open-Meteo 備援策略**

```
主要來源：CWA O-B0075-001
  • 10 個浮標站（台灣北/中/東/南/離島）
  • 即時波高、週期、風向、海溫、潮位
  • 每小時更新，但有時整批卡住

備援來源：Open-Meteo Marine API
  • 全球免費，不需 API Key
  • 任何 IP 都能存取（解決 CWA 有時限制特定 IP 的問題）
  • 無海溫、無潮位

資料決策流程：
  CWA 全部有效？ ──→ 使用 CWA
  CWA 部分缺站？ ──→ CWA 正常站 + Open-Meteo 補充缺失站
  CWA 全部失效？ ──→ 完全切換 Open-Meteo
```

**重要設計**：新鮮度檢查（> 3 小時視為失效）  
→ 颱風期間 CWA 資料可能 48 小時未更新，若不檢查會推播錯誤的平靜假象

---

### SLIDE 09｜浪況評分邏輯（一）— GoOcean 安全分級

**標題：看懂浪況：GoOcean 安全燈號**

```
參考標準：國家海洋研究院 GoOcean 官方分級

  燈號    條件                                      說明
  ──────  ────────────────────────────────────────  ──────────
  🟢 初學  浪高 < 0.8m                              安全，適合新手
  🟡 中階  浪高 ≥ 0.8m 或（浪高 ≥ 0.5m 且週期 ≥ 9s）有一定挑戰
  🔴 進階  浪高 ≥ 1.8m 或（浪高 ≥ 1.0m 且週期 ≥ 12s）需要技術
  ⛔ 危險  浪高 ≥ 3.0m 或（浪高 ≥ 2.0m 且週期 ≥ 12s）禁止入水

關鍵設計：用 OR 邏輯，而非 AND
  同樣 1.0m 的浪，週期 12s 的能量和衝擊力 >> 週期 5s
  長週期湧浪（Ground Swell）危險性不能只靠浪高判斷
```

---

### SLIDE 10｜浪況評分邏輯（二）— 品質分數 × 星號

**標題：衝浪品質 ≠ 浪高：週期才是關鍵**

```
品質分數公式：
  品質分數 = 浪高(m) × 週期(s) × 週期品質係數

週期品質係數：
  < 7s  → ×0.7  短週期風浪（Wind Swell）  雜亂、打板
  7-9s  → ×1.0  一般混合浪
  9-12s → ×1.3  長週期混合浪
  ≥ 12s → ×1.6  Ground Swell（湧浪）     乾淨有力，最佳品質

實際案例對比：
  同樣浪高 1.0m
  週期 6s（風浪）：1.0 × 6 × 0.7 = 4.2 分  ⭐⭐
  週期 12s（湧浪）：1.0 × 12 × 1.6 = 19.2 分  ⭐⭐⭐⭐⭐
  → 相同浪高，評分相差 4.6 倍

陸風加成：
  當天吹陸風（離岸風）→ 分數 × 1.3
  原因：陸風把浪「梳理」成乾淨立牆，大幅提升浪形品質
```

---

### SLIDE 11｜LINE 整合：Webhook 非同步機制

**標題：Webhook 為什麼要非同步？**

```
問題：
  查詢浪況需呼叫外部 API，可能等待 2–8 秒
  LINE 要求 Webhook 必須在 1 秒內回應 HTTP 200
  如果同步等待 → LINE 誤以為失敗 → 重送事件 → 重複推播

解法：先回 200，事件處理丟背景執行緒

  LINE 發 Webhook
       ↓
  Flask 立即回 HTTP 200  ← 這步很快（< 10ms）
       ↓（同時）
  背景執行緒：查 API → 建訊息 → 推播給用戶

額外防護：reply_token 過期時（Render 重啟中）
  reply_message() 失敗 → 自動改 push_message() 補送
```

---

### SLIDE 12｜LINE 整合：圖文選單製作原理

**標題：Rich Menu：圖片 × 按鈕分開設計**

```
一般人的誤解：「圖片上的按鈕就是按鈕」
實際上：圖片 和 按鈕座標 完全分開定義

圖片（視覺層）：
  Python Pillow 直接用程式繪製
  • 2500 × 843 像素
  • 漸層色塊（逐行填色模擬漸層）
  • Apple Color Emoji 彩色字型渲染 emoji
  • 文字黑色描邊（避免淡色背景吃字）

按鈕（行為層）：
  POST 給 LINE API 的 JSON
  • areas[] = 每格的 x/y 座標 + 動作
  • action.type = "message"
  • action.text = 觸發文字（如 "📺 浪況數據與直播資源"）

用戶點擊左下格 → LINE 自動發出觸發文字 → Webhook 收到 → 回傳 Flex Carousel
```

---

### SLIDE 13｜推播排程：Render 的睡眠問題

**標題：免費伺服器的挑戰：Render Free Tier 睡眠機制**

```
問題：
  Render 免費方案：15 分鐘無請求 → 服務進入睡眠
  冷啟動時間：30–60 秒
  結果：廣播時間到了，服務還在睡 → 廣播失敗

解法：GitHub Actions 作為外部可靠定時器

  ┌─────────────────────────────────────────┐
  │ GitHub Actions（免費，雲端執行，不受限）  │
  │                                         │
  │  broadcast.yml                          │
  │  1. 每 5 秒 curl /health               │
  │  2. 等服務醒來（最多等 2 分鐘）          │
  │  3. POST /broadcast-now 觸發廣播        │
  │                                         │
  │  keep_alive.yml                         │
  │  高峰時段每 5 分鐘 ping → 不讓它睡      │
  └─────────────────────────────────────────┘

設計取捨：
  保活時段只開台灣 05:00–10:00、14:00–20:00
  非高峰時段放它睡 → 節省免費額度（月底不會被強制停權）
```

---

### SLIDE 14｜開發挑戰（一）— CWA SSL 憑證問題

**標題：最難排查的 Bug：Render 上的 SSL 憑證失敗**

```
症狀：
  本機測試完全正常
  Deploy 到 Render 後 CWA API 呼叫全部失敗
  錯誤訊息：SSLError: Missing Subject Key Identifier

排查過程：
  ❌ 以為是 API Key 問題 → 輪替金鑰 → 沒用
  ❌ 加 requests.get(verify=False) → 仍然失敗（問題在更底層）
  ❌ 自訂 ssl.SSLContext → 仍然失敗（Python ssl 模組的限制）
  ✅ 改用系統 curl -sk 子行程 → 成功！

根本原因：
  CWA 的 HTTPS 憑證缺少 Subject Key Identifier 欄位（輕微不合規）
  本機 macOS OpenSSL 不嚴格檢查
  Render 容器 OpenSSL 3.2+ 會直接拒絕 → Python 層根本沒機會處理

學習：除錯時要辨清問題發生在哪一層，不要從症狀猜原因
```

---

### SLIDE 15｜開發挑戰（二）— 颱風期間資料過期 Bug

**標題：颱風天機器人還在推薦「適合衝浪」**

```
事件：颱風米克拉（距台 410km）期間

症狀：
  機器人推播浪況 0.3–1.3m 🟢 初學
  實際海況：1.2–2.8m 🔴 進階（用 Open-Meteo 驗證）

根本原因：
  CWA 整批浮標資料卡在 45 小時前沒更新
  程式沒有檢查資料的時間戳記（新鮮度）
  機器人把颱風前的平靜假象當成即時資料

修復方式：
  新增 MARINE_DATA_MAX_AGE_HOURS = 3
  超過 3 小時的資料 → 視為失效 → 觸發 Open-Meteo 備援

影響範圍：
  不只廣播，長浪警戒也共用同一資料源
  颱風期間該發的警戒也沒發 → 一起修復

學習：資料的「時效性」跟「存在性」同樣重要
```

---

### SLIDE 16｜系統成果

**標題：目前系統狀態**

```
✅ 已上線功能（15 項以上）：

  核心推播
  ├── 15 大浪點，每日 05:00 / 15:00 廣播
  ├── 個人化（依浪齡/板型/常去浪點客製）
  ├── 今日最佳浪點前三名推薦
  ├── 長浪警戒（自動偵測 Ground Swell）
  └── 颱風通知（1000km 範圍內）

  互動查詢
  ├── 浪點 / 地區名稱查詢 + 今日潮汐時刻
  ├── 全台 15 浪點總覽
  ├── 明日浪況預報（Open-Meteo 7 天模型）
  └── YouTube 直播快速入口（北/東/南 共 20 支）

  技術指標
  ├── Webhook 回應時間：< 10ms（非同步化後）
  ├── API 重試機制：失敗自動重試 3 次
  ├── 資料備援：CWA + Open-Meteo 混合策略
  └── 警戒 de-dup：存 DB，Render 重啟不重置
```

---

### SLIDE 17｜未來規劃

**標題：下一步要做什麼？**

```
短期（可實作）：
  🏄 find_best_spots() 加入潮汐因子
     → 滿潮前後 2 小時浪形更好，加乘排序
  📹 浪友影片投稿
     → 傳 "📹 烏石港 https://..." 自動存入 surf_videos 表
        廣播底部附上「今日直播/影片」

中期（技術升級）：
  📊 Flex Message 廣播
     → 目前純文字，改成圖文卡片更直覺
  🌐 Windguru 資料整合
     → 精準湧浪模型，補足 CWA 沒有的浪向細節

長期（商業化）：
  💰 訂閱制：免費 1 次/天，進階解鎖即時查詢無限次
  🤝 與衝浪學校合作：學員管理 + 課程浪況通知
```

---

### SLIDE 18｜結語

**標題：從想法到上線的完整旅程**

```
這個專案學到的核心能力：

  系統設計    ── 多服務協作（Render + GitHub Actions + Supabase + LINE + Streamlit）
  API 整合    ── 處理真實外部 API 的各種異常（SSL、過期、回傳 None 字串）
  資料工程    ── 資料新鮮度、備援策略、容錯機制
  非同步處理  ── Webhook 非同步化、背景執行緒
  衝浪科學    ── 湧浪物理、GoOcean 分級、陸風判斷

  「不只是會寫程式，而是用程式解決真實問題。」


掃 QR Code 試用機器人 →  [QR Code 圖片]
GitHub：https://github.com/BboyMelon/surf-robot
```

---

## 📌 製作 PPT 的補充建議

### 視覺風格
- **底色**：深海藍 `#0D4F8C` 或深色系（白字更清晰）
- **強調色**：珊瑚橙 `#F2604C`（危險/重點）+ 青藍 `#6AC6F2`（資料/安全）
- **字型**：標題用粗體，內文 18-22pt，程式碼用等寬字型

### 每張投影片原則
- **一張一個重點**，不要塞超過 5 個 bullet point
- 文字 < 圖表，能畫圖就不用文字說明
- 每張右下角可放浪的小圖示作為視覺標記

### 重點頁建議加 Demo 截圖
| 投影片 | 建議加入的截圖 |
|--------|--------------|
| SLIDE 05 | LINE 對話截圖（廣播訊息、圖文選單展開狀態）|
| SLIDE 12 | setup_richmenu.py 產出的 richmenu.png |
| SLIDE 13 | GitHub Actions 執行紀錄截圖 |

### 時間分配建議
| 段落 | 張數 | 建議時間 |
|------|------|---------|
| Part 1 問題背景 | 3 | 2 分鐘 |
| Part 2 功能展示 | 2 | 2 分鐘 |
| Part 3 技術設計 | 8 | 8 分鐘 |
| Part 4 挑戰與解法 | 2 | 3 分鐘 |
| Part 5 成果結語 | 3 | 3 分鐘 |
| **合計** | **18** | **18 分鐘** |
